# Python Scripts

`test_CellDIVE_image_DAPI_filtering.py` - Filters a segmented CellDIVE-IBEX image using the pytrendseries packge to isolate regions of curve increase on a histogram of mean DAPI intensities, then calculates the region of maximum curvature as a filtering cutoff  
